#pragma once

#include "internal_bigEndian.static"
#include "internal_byteSwap.static"

static uint16_t big_endian_short(uint16_t us)
{
   if( lemon_big_endian() == 0 )
     lemon_byte_swap(&us, sizeof(uint16_t), 1);
   return us;
}
